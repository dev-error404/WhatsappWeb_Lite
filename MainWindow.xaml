<Window x:Class="WhatsAppWebDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhatsAppWebDesktop"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="WhatsApp Web" Height="768" Width="1024"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#111b21"
        Icon="logo-whatsapp-png-46068.png">

    <Window.Resources>
        <!-- Custom styles for title bar buttons -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3d4950"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.WindowStyle>None</Window.WindowStyle>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="6" 
                      GlassFrameThickness="1"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid Grid.Row="0" Background="#202c33">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left section: Logo and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="logo-whatsapp-png-46068.png" Width="18" Height="18" Margin="10,0,8,0"/>
                <TextBlock x:Name="TxtTitle" Text="WhatsApp Lite v1.0.5" Foreground="#e9edef" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Right section: Minimize, Maximize, Close Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Downloads Button -->
                <Button x:Name="BtnDownloads" Style="{StaticResource TitleBarButtonStyle}" Click="OnDownloadsClick" ToolTip="Descargas">
                    <Viewbox Width="13" Height="13">
                        <Path Data="M19,9h-4V3H9v6H5l7,7L19,9z M5,18v2h14v-2H5z" Fill="#aebac1"/>
                    </Viewbox>
                </Button>
                <!-- Settings Button -->
                <Button x:Name="BtnSettings" Style="{StaticResource TitleBarButtonStyle}" Click="OnSettingsClick" ToolTip="Ajustes">
                    <Viewbox Width="13" Height="13">
                        <Path Data="M19.14,12.94c0.04-0.3,0.06-0.61,0.06-0.94c0-0.32-0.02-0.64-0.07-0.94l2.03-1.58c0.18-0.14,0.23-0.41,0.12-0.61 l-1.92-3.32c-0.12-0.22-0.37-0.29-0.59-0.22l-2.39,0.96c-0.5-0.38-1.03-0.7-1.62-0.94L14.4,2.81c-0.04-0.24-0.24-0.41-0.48-0.41 h-3.84c-0.24,0-0.43,0.17-0.47,0.41L9.25,5.35C8.66,5.59,8.13,5.91,7.63,6.29L5.24,5.33c-0.22-0.08-0.47,0-0.59,0.22L2.74,8.87 C2.62,9.08,2.67,9.34,2.85,9.48l2.03,1.58C4.83,11.37,4.8,11.69,4.8,12c0,0.31,0.03,0.63,0.08,0.94l-2.03,1.58 c-0.18,0.14-0.23,0.41-0.12,0.61l1.92,3.32c0.12,0.22,0.37,0.29,0.59,0.22l2.39-0.96c0.5,0.38,1.03,0.7,1.62,0.94l0.36,2.54 c0.05,0.24,0.24,0.41,0.48,0.41h3.84c0.24,0,0.44-0.17,0.47-0.41l0.36-2.54c0.59-0.24,1.13-0.56,1.62-0.94l2.39,0.96 c0.22,0.08,0.47,0,0.59-0.22l1.92-3.32c0.12-0.22,0.07-0.47-0.12-0.61L19.14,12.94z M12,15.6c-1.98,0-3.6-1.62-3.6-3.6 s1.62-3.6,3.6-3.6s3.6,1.62,3.6,3.6S13.98,15.6,12,15.6z" Fill="#aebac1"/>
                    </Viewbox>
                </Button>
                <!-- Minimize Button -->
                <Button Style="{StaticResource TitleBarButtonStyle}" Click="OnMinimizeClick">
                    <Path Data="M 0,4 L 10,4" Stroke="#aebac1" StrokeThickness="1" SnapsToDevicePixels="True"/>
                </Button>
                <!-- Maximize/Restore Button -->
                <Button x:Name="BtnMaximize" Style="{StaticResource TitleBarButtonStyle}" Click="OnMaximizeClick">
                    <Path x:Name="MaximizeIcon" Data="M 0,0 L 0,10 L 10,10 L 10,0 Z" Stroke="#aebac1" StrokeThickness="1" Fill="Transparent" SnapsToDevicePixels="True"/>
                </Button>
                <!-- Close Button -->
                <Button Style="{StaticResource CloseButtonStyle}" Click="OnCloseClick">
                    <Path Data="M 0,0 L 10,10 M 0,10 L 10,0" Stroke="#aebac1" StrokeThickness="1" SnapsToDevicePixels="True"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content (WebView2) -->
        <Grid Grid.Row="1" Background="#111b21">
            <!-- Loader / Loading background while WebView2 starts -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="LoadingPanel">
                <Image Source="logo-whatsapp-png-46068.png" Width="64" Height="64" Opacity="0.5" Margin="0,0,0,16"/>
                <ProgressBar x:Name="LoadProgress" Width="120" Height="4" IsIndeterminate="True" Background="#202c33" Foreground="#00a884" BorderThickness="0"/>
                <TextBlock x:Name="TxtLoadingStatus" Text="Iniciando..." Foreground="#8696a0" FontSize="11" Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
            
            <wv2:WebView2 x:Name="WvWhatsApp" 
                          Visibility="Collapsed" />
        </Grid>

        <!-- Settings Popup -->
        <Popup x:Name="PopSettings" StaysOpen="False" Placement="Bottom" PlacementTarget="{Binding ElementName=BtnSettings}" HorizontalOffset="-150" VerticalOffset="5">
            <Border Background="#202c33" BorderBrush="#3d4950" BorderThickness="1" CornerRadius="6" Padding="12" Width="180">
                <StackPanel>
                    <TextBlock Text="Opciones de WhatsApp Lite" Foreground="#e9edef" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <CheckBox x:Name="ChkAutoStart" Content="Iniciar con Windows" Foreground="#e9edef" FontSize="11" Click="OnAutoStartClick" IsChecked="False" Focusable="False" Margin="0,0,0,4"/>
                    <TextBlock Text="El programa se iniciará minimizado en segundo plano." Foreground="#8696a0" FontSize="9" TextWrapping="Wrap" Margin="16,0,0,0"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Window>











